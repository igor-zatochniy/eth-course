name: ci

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  go:
    name: go checks
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.26.7"
          cache: true

      - name: Download modules
        run: go mod download

      - name: Check formatting
        run: test -z "$(find . -name '*.go' -not -path './vendor/*' -print0 | xargs -0 gofmt -l)"

      - name: Test
        run: go test ./...

      - name: Vet
        run: go vet ./...

      - name: Race test
        run: go test -race ./...

      - name: Govulncheck
        run: go run golang.org/x/vuln/cmd/govulncheck@v1.5.0 ./...

  lint:
    name: golangci-lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.26.7"
          cache: true

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.12.0

  integration:
    name: postgres integration
    runs-on: ubuntu-latest
    timeout-minutes: 25
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: cryptopulse
          POSTGRES_USER: cryptopulse_user
          POSTGRES_PASSWORD: integration_password
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U cryptopulse_user -d cryptopulse"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.26.7"
          cache: true

      - name: Download modules
        run: go mod download

      - name: Verify database migrations
        env:
          DATABASE_URL: postgres://cryptopulse_user:integration_password@127.0.0.1:5432/cryptopulse?sslmode=disable
        run: |
          go run github.com/pressly/goose/v3/cmd/goose@v3.25.0 -dir migrations postgres "$DATABASE_URL" up
          go run github.com/pressly/goose/v3/cmd/goose@v3.25.0 -dir migrations postgres "$DATABASE_URL" status

      - name: Run PostgreSQL integration tests
        env:
          INTEGRATION_DATABASE_URL: postgres://cryptopulse_user:integration_password@127.0.0.1:5432/cryptopulse?sslmode=disable
        run: go test -tags=integration ./...

  docker:
    name: docker build
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Build image
        run: docker build --pull -t cryptopulse-telegram-bot-ci .

  secrets:
    name: secret scan
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Scan complete Git history
        uses: docker://ghcr.io/gitleaks/gitleaks:v8.24.3
        with:
          args: git --redact --verbose --log-opts=--all .
        env:
          GIT_CONFIG_COUNT: "1"
          GIT_CONFIG_KEY_0: safe.directory
          GIT_CONFIG_VALUE_0: /github/workspace
